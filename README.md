# Бачурин Никита ИТС-2 Лабораторная №7

# Задание 1

## Задача 11

### Текст задачи

В файле найти среднее арифметическое максимального и минимального элементов.

### Алгоритм решения

1. вызывается метод GenerateFile1
2. создаётся генератор случайных чисел
3. создаётся объект записи в файл
4. выполняется цикл заданное количество раз
5. генерируется случайное целое число
6. число записывается в файл в новую строку
7. после завершения цикла файл закрывается
8. вызывается метод Task1_AverageMinMax
9. открывается файл для чтения
10. считывается первая строка
11. значение преобразуется в число
12. переменные min и max инициализируются этим значением
13. выполняется цикл до конца файла
14. текущая строка преобразуется в число
15. если число меньше min, обновляется min
16. если число больше max, обновляется max
17. считывается следующая строка
18. после завершения цикла файл закрывается
19. вычисляется среднее арифметическое min и max
20. результат возвращается


# Задание 2

## Задача 11

### Текст задачи

Вычислить произведение чётных элементов.

### Алгоритм решения

1. вызывается метод GenerateFile2
2. создаётся генератор случайных чисел
3. создаётся объект записи в файл
4. выполняется внешний цикл по строкам
5. выполняется внутренний цикл по элементам строки
6. генерируется случайное число
7. число записывается в файл с пробелом
8. после строки выполняется переход на новую строку
9. после завершения циклов файл закрывается
10. вызывается метод Task2_ProductEven
11. открывается файл для чтения
12. инициализируется переменная произведения
13. устанавливается флаг наличия чётных чисел
14. выполняется цикл чтения строк
15. строка разбивается на элементы
16. выполняется цикл по элементам строки
17. элемент преобразуется в число
18. если число чётное, умножается на произведение
19. устанавливается флаг наличия чётных чисел
20. считывается следующая строка
21. после завершения файл закрывается
22. если чётных чисел нет, возвращается 0
23. иначе возвращается произведение


# Задание 3

## Задача 11

### Текст задачи

Переписать в другой файл строки, в которых нет латинских букв.

### Алгоритм решения

1. вызывается метод GenerateFile3
2. создаётся объект записи в файл
3. записываются текстовые строки
4. файл закрывается
5. вызывается метод Task3_FilterLines
6. открывается входной файл для чтения
7. создаётся выходной файл для записи
8. выполняется цикл чтения строк
9. для строки устанавливается флаг наличия латинских букв
10. выполняется цикл по символам строки
11. если символ является латинской буквой, устанавливается флаг
12. после проверки строки
13. если латинских букв нет, строка записывается в выходной файл
14. считывается следующая строка
15. после завершения оба файла закрываются


# Задание 4

## Задача 11

### Текст задачи

Найти количество квадратов нечетных чисел среди компонент файла.

### Алгоритм решения

1. вызывается метод GenerateBinaryFile4
2. создаётся генератор случайных чисел
3. создаётся бинарный файл для записи
4. выполняется цикл заданное количество раз
5. генерируется случайное число
6. число записывается в бинарный файл
7. файл закрывается
8. вызывается метод Task4_CountOddSquares
9. открывается бинарный файл для чтения
10. инициализируется счётчик
11. выполняется цикл до конца файла
12. считывается число
13. проверяется, является ли число нечётным
14. если число нечётное, вычисляется квадрат
15. увеличивается счётчик
16. после завершения файл закрывается
17. результат возвращается


# Задание 5

## Задача 11

### Текст задачи

Файл содержит сведения об игрушках: название игрушки, ее стоимость в рублях и возрастные
границы (например, игрушка может предназначаться для детей от двух до пяти лет). Получить
название самой дорогой игрушки для детей до четырех лет.

### Алгоритм решения

1. вызывается метод GenerateToyFile
2. создаётся массив структур Toy
3. для каждого элемента задаются значения полей
4. создаётся объект сериализации
5. открывается файл для записи
6. выполняется сериализация массива в файл
7. файл закрывается
8. вызывается метод Task5_MostExpensiveToy
9. создаётся объект десериализации
10. открывается файл для чтения
11. выполняется десериализация массива
12. файл закрывается
13. инициализируется переменная максимальной цены
14. инициализируется переменная для имени
15. выполняется цикл по массиву структур
16. проверяется условие возраста
17. если цена больше текущего максимума, обновляется максимум
18. сохраняется имя
19. после завершения цикла результат возвращается

# Задание 6

## Задача 11

### Текст задачи

Составить программу, которая формирует список L, включив в него по одному разу элементы,
которые входят одновременно в оба списка L1 и L2.

### Алгоритм решения

1. метод Task6_Intersection принимает два списка list1 и list2.
2. создаётся пустой список result для хранения результата.
3. выполняется цикл по элементам списка list1.
4. берётся текущий элемент value из list1.
5. проверяется наличие value в списке list2.
6. если элемент найден в list2, он считается общим.
7. проверяется, есть ли value уже в result.
8. если элемента нет в result, он добавляется.
9. после завершения цикла возвращается список result.

# Задание 7

## Задача 11

### Текст задачи

В конец непустого списка L добавить все его элементы, располагая их в обратном порядке
(например, по списку из элементов 1, 2, 3 требуется построить список из элементов 1, 2, 3, 3, 2, 1).

### Алгоритм решения

1. метод Task7_ReverseAppend принимает LinkedList list.
2. создаётся временный список temp.
3. выполняется обход LinkedList с начала до конца.
4. каждый элемент добавляется в temp.
5. после обхода выполняется цикл по temp в обратном порядке.
6. элементы из temp добавляются в конец LinkedList.
7. список дополняется своими элементами в обратном порядке.

# Задание 9

## Задача 11

### Текст задачи

Файл содержит текст на русском языке. Сколько разных букв встречается в тексте?

### Алгоритм решения

1. метод Task9_CountDifferentLetters принимает путь к файлу.
2. проверяется существование файла через File.Exists.
3. если файл не существует, метод завершается.
4. создаётся HashSet для хранения уникальных букв.
5. файл открывается для чтения.
6. выполняется построчное чтение файла.
7. каждая строка обрабатывается посимвольно.
8. если символ является русской буквой, он добавляется в HashSet.
9. повторяющиеся буквы автоматически игнорируются.
10. после завершения чтения возвращается количество элементов HashSet.

### Тестирование